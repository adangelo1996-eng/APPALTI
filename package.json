{
  "name": "rfp-ai-copilot-monorepo",
  "private": true,
  "scripts": {
    "dev": "cd apps/web && npx next dev",
    "build": "cd apps/web && npx prisma generate && npx next build",
    "start": "cd apps/web && npx next start",
    "lint": "cd apps/web && npx next lint",
    "prisma:generate": "cd apps/web && npx prisma generate",
    "prisma:push": "cd apps/web && npx prisma db push",
    "prisma:studio": "cd apps/web && npx prisma studio"
  }
}

